#include <bits/stdc++.h>
 
using namespace std;
 
typedef long long ll;
 
#define el '\n'
#define MOD 1000000007
 
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    set<string> st;
    string s;
    getline()
        if(st.count(s)){
            cout << "no" << el;
            return 0;
        } else {
            st.insert(s);
        }
    }
    cout << "yes" << el;
    return 0;
}